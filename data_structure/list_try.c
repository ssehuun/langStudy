#include <stdio.h>
#include <stdlib.h>

typedef struct node{
	int value;
	node* next;
}mynode;

typedef struct list{
	node* head;
	node* last;
	int size;
}mylist;

void init_list(list* mylist);
void push_back(list* mylist, int val);
int get_idx(list* mylist, int idx);
int get_size(list* mylist);
int get_count(list* mylist, int x);
void clear(mylist* list);

int main(){
	int n;
	scanf("%d", &n);
	while(n--){
		init_
		char* cmd;
		scanf("%s", cmd);
		if(s == "s"){
			
		}
	}
}


